import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ng.clickshop',
  appName: 'ng-clickshop',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
